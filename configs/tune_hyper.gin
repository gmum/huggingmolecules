# ==============================================================================
study_name = 'TPE hps search'
optuna.study_name = %study_name
neptune.experiment_name = %study_name
train.use_neptune = True

# ==============================================================================
optuna.storage = 'sqlite:///optuna_studies.db'
optuna.save_path = 'experiments/tune_hyper_0'
optuna.keep_best_only = True
ModelCheckpoint.save_last = False

# ==============================================================================
optuna.direction = 'minimize'
optuna.metric = 'valid_loss'
optuna.sampler_name = 'TPESampler'
optuna.n_trials = 300
optuna.params = {
    'optimizer.lr': ('suggest_loguniform', 0.000001, 0.001),
    'NoamLRScheduler.warmup_factor': ('suggest_uniform', 0.0, 0.5)
}

# ==============================================================================
optuna.resume = True


