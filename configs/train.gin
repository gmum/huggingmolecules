# ==============================================================================
optimizer.name = 'Adam'
optimizer.lr = 0.001

# ==============================================================================
lr_scheduler.name = 'FancyScheduler'
lr_scheduler.model_dim = 1024
lr_scheduler.warmup = 10

# ==============================================================================
loss_fn.name = 'mse_loss'

# ==============================================================================
train.gpus = [1]
train.num_epochs = 13
train.batch_size = 9
train.save_path = './experiments/test_training'
train.use_neptune = True
train.resume = False

# ==============================================================================
data.data_path = './data/freesolv/freesolv.csv'
data.num_workers = 4
data.train_size = 0.9
data.seed = None

# ==============================================================================
neptune.user_name = 'panpiotr8'
neptune.project_name = 'sandbox'
neptune.experiment_name = 'first gpu experiment'

